# ============================================================
# SkillBridge AI — Backend Dependencies
# Python 3.11+
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12        # untuk upload file (CV)

# ── Data Validation ──────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.7.0

# ── LLM — Gemini 3.0 Flash ───────────────────────────────────
google-generativeai

# ── NLP / Semantic Search ─────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.3.1    # SBERT: paraphrase-multilingual-MiniLM-L12-v2
torch==2.5.1                    # PyTorch (CPU) — backend untuk sentence-transformers
transformers==4.47.1            # Hugging Face transformers
faiss-cpu==1.9.0                # Vector similarity search (FAISS, lokal/MVP)

# ── Machine Learning ──────────────────────────────────────────
scikit-learn==1.6.0             # LogisticRegression, RandomForest, cosine_similarity
numpy==1.26.4                   # array operations, polyfit, clip
joblib==1.4.2                   # model serialization (save/load .pkl)
xgboost==2.1.3                  # [planned] upgrade dari LogisticRegression — belum dipakai saat ini

# ── Database — Supabase ──────────────────────────────────────
supabase                # Supabase Python client
postgrest               # Supabase REST client

# ── Environment & Config ──────────────────────────────────────
python-dotenv==1.0.1            # load .env file

# ── HTTP Client ───────────────────────────────────────────────
httpx                   # async HTTP (digunakan oleh Supabase & Anthropic)
requests==2.32.3                # sync HTTP (scraping, SerpApi)

# ── Job Scraping ──────────────────────────────────────────────
google-search-results==2.4.2    # SerpApi SDK untuk scraping Google Jobs
beautifulsoup4==4.12.2          # HTML parsing

# ── Data Processing ───────────────────────────────────────────
pandas==2.2.3                   # DataFrame processing (opsional, untuk Prophet/trend)

# ── Logging & Monitoring ─────────────────────────────────────
python-json-logger==3.2.1       # structured JSON logging (opsional)

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2                   # async test client FastAPI (TestClient)

# PDF Reader
Pillow==11.0.0                  # Image manipulation for OCR
pdf2image==1.17.0               # PDF to Image conversion

# Google Cloud Platform
google-cloud-aiplatform==1.46.0
google-cloud-storage==2.19.0     # Cloud Storage for CV assets

# -- Async & Performance --------------------------------------
asyncpg==0.30.0                  # Async PostgreSQL driver
redis==5.2.0                     # Caching & Task Broker

# -- Task Queue (Asynchronous ETL) ----------------------------
celery==5.4.0                    # Distributed Task Queue


flashtext

